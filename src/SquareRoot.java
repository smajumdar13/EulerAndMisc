public class SquareRoot {
    private double l, h, mid, square;

    public double squareRoot(double n) {
        square = mid * mid;
        if (square == n) {
            return mid;
        }
        return mid;
    }
}
